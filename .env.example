# Ejemplo de variables de entorno necesarias
# Copia este archivo a `.env` y completa los valores en tu entorno local/CI.

# Base de datos
# Para producción usar una URL Postgres
DATABASE_URL=postgresql://user:password@localhost:5432/docgus
TEST_DB_URL=postgresql://user:password@localhost:5432/docgus_test

# Forzar sqlite localmente (opcional)
# FORCE_SQLITE=1

# JWT / Secrets
JWT_SECRET_KEY=change-me-to-a-secure-random-value
FLASK_SECRET_KEY=change-me-to-a-secure-random-value

# Cloudinary (opcional) - si usas Cloudinary para uploads
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Frontend: URL pública del backend que usa Vite durante desarrollo
VITE_BACKEND_URL=http://localhost:3001/api

# Opt-in flags
AUTO_CREATE_SCHEMA=1
MIGRATE_ON_START=0
MIGRATE_FROM_URL=
# .env.example - variables de entorno esperadas
#
# Uso:
# - Backend: os.getenv('VARIABLE_NAME')
# - Frontend (Vite): import.meta.env.VITE_* (ej. VITE_BACKEND_URL)
#
# Copia este archivo a `.env` y rellena los valores sensibles. NO subir secretos a git.

# ---------------------- Back-end ----------------------
# DB: Postgres ejemplo: postgres://user:pass@host:5432/dbname
# SQLite dev: sqlite:////absolute/path/to/dev.db
DATABASE_URL=
TEST_DB_URL=

# Forzar SQLite en entornos locales/CI (1=true)
FORCE_SQLITE=0

# Auto crear schema si se usa SQLite (1=true)
AUTO_CREATE_SCHEMA=1

# JWT secret (OBLIGATORIO en producción)
JWT_SECRET_KEY=

# Secret para sesiones y Flask-Admin (opcional, fallback)
FLASK_APP_KEY=

# Control de debug (0/1)
FLASK_DEBUG=1

# ---------------------- Cloudinary (opcional) ----------------------
# Formato: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ---------------------- Migraciones / migración legacy ----------------------
MIGRATE_ON_START=0
MIGRATE_FROM_URL=

# ---------------------- Front-end (Vite) ----------------------
# El front usa import.meta.env.VITE_BACKEND_URL para llamar al backend en dev
VITE_BACKEND_URL=http://localhost:5000
VITE_BASENAME=/

# ---------------------- Notes ----------------------
# - Rellena JWT_SECRET_KEY antes de desplegar en producción.
# - Para pruebas locales rápidas puedes dejar FORCE_SQLITE=1 y AUTO_CREATE_SCHEMA=1
